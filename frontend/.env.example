# Backend base URL for PRODUCTION (your Render service), e.g.
#   VITE_API_BASE_URL=https://recruitai-backend.onrender.com
# Leave EMPTY for local dev — the Vite proxy forwards /api to localhost:3024.
VITE_API_BASE_URL=
